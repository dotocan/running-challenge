version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    # User requested NO restart policy (or restart: no)
    restart: no
    volumes:
      - ./running-challenge-redis-data:/data
